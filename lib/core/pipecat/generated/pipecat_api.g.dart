// Autogenerated from Pigeon (v26.3.3), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: unused_import, unused_shown_name
// ignore_for_file: type=lint

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List;

import 'package:flutter/services.dart';
import 'package:meta/meta.dart' show immutable, protected, visibleForTesting;

Object? _extractReplyValueOrThrow(
    List<Object?>? replyList,
    String channelName, {
    required bool isNullValid,
}) {
  if (replyList == null) {
    throw PlatformException(
      code: 'channel-error',
      message: 'Unable to establish connection on channel: "$channelName".',
    );
  } else if (replyList.length > 1) {
    throw PlatformException(
      code: replyList[0]! as String,
      message: replyList[1] as String?,
      details: replyList[2],
    );
  } else if (!isNullValid && (replyList.isNotEmpty && replyList[0] == null)) {
    throw PlatformException(
      code: 'null-error',
      message: 'Host platform returned null value for non-null return value.',
    );
  }
  return replyList.firstOrNull;
}

bool _deepEquals(Object? a, Object? b) {
  if (identical(a, b)) {
    return true;
  }
  if (a is double && b is double) {
    if (a.isNaN && b.isNaN) {
      return true;
    }
    return a == b;
  }
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed
            .every(((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]));
  }
  if (a is Map && b is Map) {
    if (a.length != b.length) {
      return false;
    }
    for (final MapEntry<Object?, Object?> entryA in a.entries) {
      bool found = false;
      for (final MapEntry<Object?, Object?> entryB in b.entries) {
        if (_deepEquals(entryA.key, entryB.key)) {
          if (_deepEquals(entryA.value, entryB.value)) {
            found = true;
            break;
          } else {
            return false;
          }
        }
      }
      if (!found) {
        return false;
      }
    }
    return true;
  }
  return a == b;
}

int _deepHash(Object? value) {
  if (value is List) {
    return Object.hashAll(value.map(_deepHash));
  }
  if (value is Map) {
    int result = 0;
    for (final MapEntry<Object?, Object?> entry in value.entries) {
      result += (_deepHash(entry.key) * 31) ^ _deepHash(entry.value);
    }
    return result;
  }
  if (value is double && value.isNaN) {
    // Normalize NaN to a consistent hash.
    return 0x7FF8000000000000.hashCode;
  }
  if (value is double && value == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return 0.0.hashCode;
  }
  return value.hashCode;
}


/// Maps to ai.pipecat.client.types.TransportState on Android / iOS.
enum PipecatTransportState {
  idle,
  initializing,
  initialized,
  authenticating,
  connecting,
  connected,
  ready,
  disconnecting,
  disconnected,
  error,
}

/// Discriminator for [PipecatEventData]. Each value corresponds to a
/// PipecatEventCallbacks method on the native SDK.
enum PipecatEventType {
  /// onUserTranscript — Whisper STT via TranscriptionFrame
  userTranscript,
  /// onBotTranscript — final bot reply text
  botTranscript,
  /// onBotLLMText — streaming LLM token chunks (LLMTextFrame)
  botLlmText,
  /// onBotLLMStarted — LLMFullResponseStartFrame
  botLlmStarted,
  /// onBotLLMStopped — LLMFullResponseEndFrame
  botLlmStopped,
  /// onBotTTSStarted — TTSStartedFrame
  botTtsStarted,
  /// onBotTTSStopped — TTSStoppedFrame
  botTtsStopped,
  /// onBotStartedSpeaking — audio playback began
  botStartedSpeaking,
  /// onBotStoppedSpeaking — audio playback ended
  botStoppedSpeaking,
  /// onUserStartedSpeaking — Silero VAD rising edge
  userStartedSpeaking,
  /// onUserStoppedSpeaking — Silero VAD falling edge
  userStoppedSpeaking,
  /// onBotReady — pipeline fully initialized
  botReady,
  /// onBotConnected
  botConnected,
  /// onBotDisconnected
  botDisconnected,
  /// onConnected — transport layer connected
  transportConnected,
  /// onDisconnected — transport layer disconnected
  transportDisconnected,
  /// onTransportStateChanged — generic state transition
  transportStateChanged,
  /// onBackendError — ErrorFrame from pipeline
  backendError,
}

/// Parameters required to start a Pipecat session.
///
/// [baseUrl] is the voice server origin (e.g. `https://voicebanking.example.com`).
/// The Pipecat SmallWebRTC transport calls `POST {baseUrl}/start`, then negotiates
/// SDP at `{baseUrl}/sessions/{sessionId}/api/offer` (handled inside the native SDK).
/// [customBodyJson] is a JSON-encoded string for extra parameters (avoids
/// Pigeon limitations with nested Object maps).
class SessionConfig {
  SessionConfig({
    required this.baseUrl,
    this.enableMic = true,
    this.enableCam = false,
    this.customBodyJson,
    this.requestHeadersJson,
  });

  /// Voice / SmallWebRTC server origin (scheme + host, no trailing slash).
  String baseUrl;

  bool enableMic;

  bool enableCam;

  /// JSON-encoded Map<String,dynamic> of extra session parameters.
  /// Passed as `request_data` where supported by the transport.
  String? customBodyJson;

  /// JSON object of HTTP header names to values, e.g. `{"Authorization":"Bearer …"}`.
  /// Sent on `/start` and offer POSTs when supported by the native transport.
  String? requestHeadersJson;

  List<Object?> _toList() {
    return <Object?>[
      baseUrl,
      enableMic,
      enableCam,
      customBodyJson,
      requestHeadersJson,
    ];
  }

  Object encode() {
    return _toList();  }

  static SessionConfig decode(Object result) {
    result as List<Object?>;
    return SessionConfig(
      baseUrl: result[0]! as String,
      enableMic: result[1]! as bool,
      enableCam: result[2]! as bool,
      customBodyJson: result[3] as String?,
      requestHeadersJson: result[4] as String?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! SessionConfig || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(baseUrl, other.baseUrl) && _deepEquals(enableMic, other.enableMic) && _deepEquals(enableCam, other.enableCam) && _deepEquals(customBodyJson, other.customBodyJson) && _deepEquals(requestHeadersJson, other.requestHeadersJson);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);
}

/// Flat, Pigeon-safe event envelope streamed from native → Flutter.
///
/// The [type] discriminator determines which nullable fields are populated.
/// Flutter-side [PipecatEvent] sealed classes provide a type-safe façade.
class PipecatEventData {
  PipecatEventData({
    required this.type,
    this.text,
    this.isFinal,
    this.userId,
    this.language,
    this.timestamp,
    this.transportState,
    this.errorMessage,
  });

  PipecatEventType type;

  /// Transcript text (user or bot) / LLM chunk text.
  String? text;

  /// True when the transcript is finalized (not interim).
  bool? isFinal;

  /// Participant / user ID for the transcript source.
  String? userId;

  /// Detected language code (e.g. "en", "hi") from LanguageAnnotator.
  String? language;

  /// ISO-8601 timestamp of the transcription.
  String? timestamp;

  /// Populated only for [PipecatEventType.transportStateChanged].
  PipecatTransportState? transportState;

  /// Populated only for [PipecatEventType.backendError].
  String? errorMessage;

  List<Object?> _toList() {
    return <Object?>[
      type,
      text,
      isFinal,
      userId,
      language,
      timestamp,
      transportState,
      errorMessage,
    ];
  }

  Object encode() {
    return _toList();  }

  static PipecatEventData decode(Object result) {
    result as List<Object?>;
    return PipecatEventData(
      type: result[0]! as PipecatEventType,
      text: result[1] as String?,
      isFinal: result[2] as bool?,
      userId: result[3] as String?,
      language: result[4] as String?,
      timestamp: result[5] as String?,
      transportState: result[6] as PipecatTransportState?,
      errorMessage: result[7] as String?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! PipecatEventData || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(type, other.type) && _deepEquals(text, other.text) && _deepEquals(isFinal, other.isFinal) && _deepEquals(userId, other.userId) && _deepEquals(language, other.language) && _deepEquals(timestamp, other.timestamp) && _deepEquals(transportState, other.transportState) && _deepEquals(errorMessage, other.errorMessage);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);
}


class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();
  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    }    else if (value is PipecatTransportState) {
      buffer.putUint8(129);
      writeValue(buffer, value.index);
    }    else if (value is PipecatEventType) {
      buffer.putUint8(130);
      writeValue(buffer, value.index);
    }    else if (value is SessionConfig) {
      buffer.putUint8(131);
      writeValue(buffer, value.encode());
    }    else if (value is PipecatEventData) {
      buffer.putUint8(132);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129:
        final value = readValue(buffer) as int?;
        return value == null ? null : PipecatTransportState.values[value];
      case 130:
        final value = readValue(buffer) as int?;
        return value == null ? null : PipecatEventType.values[value];
      case 131:
        return SessionConfig.decode(readValue(buffer)!);
      case 132:
        return PipecatEventData.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

const StandardMethodCodec pigeonMethodCodec = StandardMethodCodec(_PigeonCodec());

/// Commands sent from Flutter to the native Pipecat SDK.
class PipecatHostApi {
  /// Constructor for [PipecatHostApi].  The [binaryMessenger] named argument is
  /// available for dependency injection.  If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  PipecatHostApi({BinaryMessenger? binaryMessenger, String messageChannelSuffix = ''})
      : pigeonVar_binaryMessenger = binaryMessenger,
        pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// Instantiate the native PipecatClient with the given session config.
  /// Must be called before [start]. Runs SDK init on a background thread.
  Future<void> initialize(SessionConfig config) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.initialize$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[config]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Initiate SmallWebRTC SDP handshake; bot spawns on connection.
  Future<void> start() async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.start$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Disconnect and release the native client resources.
  Future<void> stop() async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.stop$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Send a typed RTVI action to the bot (future extensibility).
  /// [dataJson] is a JSON-encoded payload to avoid Pigeon Object limitations.
  Future<void> sendAction(String actionType, String dataJson) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.sendAction$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[actionType, dataJson]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Toggle microphone on/off without tearing down the session.
  Future<void> enableMic(bool enable) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.enableMic$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[enable]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Toggle camera on/off (unused for voice-only, provided for completeness).
  Future<void> enableCam(bool enable) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.voice_banking_mobile.PipecatHostApi.enableCam$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[enable]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }
}

/// Type-safe EventChannel delivering [PipecatEventData] from native SDK
/// callbacks to Flutter. Pigeon generates `Stream<PipecatEventData>` on
/// the Dart side and a StreamHandler interface on the native side.
Stream<PipecatEventData> streamEvents( {String instanceName = ''}) {
  if (instanceName.isNotEmpty) {
    instanceName = '.$instanceName';
  }
  final EventChannel streamEventsChannel =
      EventChannel('dev.flutter.pigeon.voice_banking_mobile.PipecatEventChannelApi.streamEvents$instanceName', pigeonMethodCodec);
  return streamEventsChannel.receiveBroadcastStream().map((dynamic event) {
    return event as PipecatEventData;
  });
}
    
